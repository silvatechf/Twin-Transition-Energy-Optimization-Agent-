import { Routes } from '@angular/router';
import { AppComponent } from './app.component';

/**
 * Definição de rotas para a aplicação Angular Standalone.
 * Nota: Como esta é uma aplicação de Dashboard de componente único (MVP), 
 * o array de rotas será deixado vazio ou configurado para a rota principal.
 */
export const routes: Routes = [
    // Se você precisar de uma rota de entrada (opcional, mas boa prática):
    // { path: '', component: AppComponent, title: 'Optimization Dashboard' }
];
